{
  "name": "merch-monorepo",
  "private": true,
  "workspaces": ["apps/web", "apps/mobile", "shared", "firebase"],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "start:mobile": "npm run start --workspace=apps/mobile"
  }
}
